#pragma once

#include <chrono>
#include <iostream>
#include <thread>
#include <string>
#include <limits>
#include <iterator>

bool isStringValidInt(std::string &str);
void screenClear();
void screenSleep(int seconds = 1);